using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ButtonHandler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Get the button it's assigned to
        //Get the current character to know what the skill will actually do.
        //change UI maybe
    }

    // Update is called once per frame
    void Update()
    {
        

    }


    //Event OnButtonClick

    //define event here, game manager will listen to event 
}
